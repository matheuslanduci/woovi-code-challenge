import { AddAccountMutation } from './AddAccountMutation'
import { RefreshAccountBalanceMutation } from './RefreshAccountBalanceMutation'

export const accountMutations = {
  AddAccount: AddAccountMutation,
  RefreshAccountBalance: RefreshAccountBalanceMutation
}
